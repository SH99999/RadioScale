# Scale Radio Tuner

Purpose: tuner runtime and renderer behavior.

- work lane: `dev/tuner`
- deploy path: Git-driven workflows + repo scripts
- rollback: repo-controlled

Keep changes focused on tuner behavior, runtime stability, and deploy safety.
