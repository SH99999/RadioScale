# Scale Radio Bridge

Purpose: metadata/provider bridge layer.

- work lane: `dev/bridge`
- deploy path: Git-driven workflows + repo scripts
- rollback: repo-controlled

Keep changes focused on bridge/runtime reliability and metadata behavior.
