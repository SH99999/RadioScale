# Scale Radio Hardware

Purpose: hardware-facing integration constraints.

- work lane: `dev/hardware`
- deploy path: Git-driven workflows + repo scripts (when active)
- rollback: repo-controlled

Keep hardware assumptions explicit and fail safely when unavailable.
