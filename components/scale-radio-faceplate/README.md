# Scale Radio Faceplate

Purpose: legacy/reference faceplate material.

- not an active deploy component in this repo model
- if promoted to active work, route through `dev/ux`

Do not treat this folder as runtime truth.
