# Scale Radio AutoSwitch

Purpose: automatic runtime mode/source switching.

- work lane: `dev/autoswitch`
- deploy path: Git-driven workflows + repo scripts
- rollback: repo-controlled

Keep changes focused on deterministic switching behavior.
